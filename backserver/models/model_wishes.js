/// (250102 누리) wish sequelize 모델 정의. 이거 없으면 쿼리문 써야해서 걍 만든다.

const Sequelize = require('sequelize');

class Wishes extends Sequelize.Model {
    Wishes.init(
        {
            account_id : 
        }
    )
}